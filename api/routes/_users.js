// Importar Express (para lidar com rotas HTTP).


// Importar as funções controladoras de usuários para cada operação CRUD (criada em user.js)


// Criar um Express roteador (para lidar com as rotas da API de usuários abaixo / indicar que vai ser uma rota)


// Rota para obter / pegar. Chamar na raíz a função pegar usuários (quando uma requisição GET é feita na raíz).


// Rota para adicionar. Chamar na raíz a função adicionar usuários (quando uma requisição POST é feita na raíz).


// Rota para atualizar. Chamar na raíz com base no id a função atualizar usuários (quando uma requisição PUT é feita na raíz com base no id).


// Rota para deletar. Chamar na raíz com base no id a função deletar usuários (quando uma requisição DELETE é feita na raíz com base no id).


// Exportar por padrão o roteador (para ser usado em outras partes do aplicativo).